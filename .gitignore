# Build artifacts (skill binaries are built via `make`)
/bin/

# Per-machine toggle map (gitignored per AGENTS.md)
skills.local.json

# Secrets — never commit (placed next to skill binaries)
.env
**/.env

# JS/TS dependencies (built inside extension dirs, exposed via symlink)
node_modules/

# OS / editor cruft
.DS_Store

.playwright-mcp/